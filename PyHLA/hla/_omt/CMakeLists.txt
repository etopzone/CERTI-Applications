INCLUDE_DIRECTORIES(${PYTHON_INCLUDE_PATH})

ADD_LIBRARY(_omt SHARED
    basicdata.h
    basicdata.cpp
    module.h
    module.cpp)

SET_TARGET_PROPERTIES(_omt PROPERTIES PREFIX "")
IF (WIN32)
SET_TARGET_PROPERTIES(_omt PROPERTIES SUFFIX ".pyd")
ENDIF (WIN32)

TARGET_LINK_LIBRARIES(_omt
    ${PYTHON_LIBRARY})

INSTALL(TARGETS _omt
    LIBRARY DESTINATION ${SITE_PACKAGES}
    ARCHIVE DESTINATION ${SITE_PACKAGES})

# $Id: CMakeLists.txt,v 1.1 2008/09/25 17:17:31 gotthardp Exp $
